# 2026-08-16 工作日志

## 桌面版修复：中文编码兼容 + Chrome 探测增强 + 重新打包

**背景**：用户把桌面版 zip 发给别人，对方电脑上：① 视频渲染报 `Browser was not found at the configured executable Path (C:/Program Files/Google/Chrome/Application/chrome.exe)`；② 早前有中文乱码问题。

### 排查过程（先质疑后执行）
1. **Chrome 报错**：`desktop/main.js` 的 `findChromePath()` 只探测 3 个常见路径，对方机器 Chrome 不在候选 → 落到**兜底默认值**（写死的第一条路径），puppeteer 找不到。报错路径恒为默认值，不是真实探测结果。确认是**探测覆盖不足**，不是"读取路径"本身错。
2. **依赖澄清**：视频导出 + omofuna 同步共用 `videotagger.render.chrome-path`（yml `${RENDER_CHROME_PATH:默认}`），puppeteer-core 只需 **Chromium 内核**，不必非 Chrome——**Win10/11 预装 Edge 可兜底**。AniList 同步走纯 HTTP，不依赖浏览器。
3. **乱码修复状态**：源码已改（`application.yml` 加 `spring.sql.init.encoding: UTF-8`；`main.js` 加 JVM `-Dfile.encoding=UTF-8`），但 **release zip 是修复前打的**——实测 zip 内 jar 的 application.yml 无 encoding 修复、app.asar 无 -Dfile 修复，发出去的包乱码原样还在。

### 改动
- **`desktop/main.js`** `findChromePath()` 升级：
  - 新增 `queryAppPath(name)`——注册表 App Paths 查 `chrome.exe`/`msedge.exe`（HKLM 64 位 + WOW6432Node 32 位），reg query 解析按 `REG_SZ` 类型取路径（兼容中文 locale "(默认)" 本地化）
  - 顺序：环境变量 → 注册表 Chrome → 常见路径 → **Edge 回退**（注册表 + 标准路径）→ 默认兜底
  - 本机验证：注册表命中 Chrome + Edge，中文系统解析正常
- **`backend/src/main/resources/application.yml`**：`spring.sql.init.encoding: UTF-8`（前置已有改动，本次一并提交）

### 重新打包（含两个修复）
- 前置缺失：`desktop/resources/`（jre/node/ffmpeg）和 `node_modules` 上次打包后被清理 → 从**旧 release zip 提取 jre+node 复用**（291MB jre 免重新下载）+ `tools/ffmpeg.exe` + 镜像 npm install electron/electron-builder
- 跑 `pack.bat`：mvn 重建 jar ✅、electron-builder 出 zip ✅，但 **`[6/6]` 失败**——旧 `release/` 里有个损坏的 `data.bak`（上次打包后直接运行 release 内 exe 产生的空目录，NTFS 报 corrupted），`rmdir` 删不掉 → `move win-unpacked` 失败，win-unpacked 被移进 `release/VideoTagger/` 子目录
- 收尾方案（不删旧 release 交付物）：从 `release/VideoTagger/win-unpacked/` 直接压缩新发布 zip → **`release-new/video-tagger-desktop-0.1.0.zip`**（510MB，3803 文件，`VideoTagger/` 外层结构）
- **新包实测验证**：jar 内 application.yml `encoding UTF-8 = True`；app.asar `-Dfile.encoding=UTF-8` + `queryAppPath` + `msedge 回退` 全 True

### 提交
- `82b5893` fix: 桌面版乱码修复（sql.init + JVM 显式 UTF-8）+ Chrome 探测增强（注册表 App Paths + Edge 回退）

### 遗留
- **旧 `release/` 里的旧 zip（发给别人的那份）未动**——删除旧交付物需用户确认；新包在 `release-new/`，用户决定是否替换进 `release/`
- `desktop/resources/`（jre/node/ffmpeg 291MB+）重新生成但被 gitignore，下次打包前需保留（pack.bat 检查缺 jre 会 ERROR）

## 在线更新方案 + 阶段① SQLite schema 迁移框架

**背景**：用户问"打新包能解决表结构更新吗"→ 分析出：桌面版 Flyway 禁用、schema 靠 spring.sql.init 跑 `CREATE TABLE IF NOT EXISTS`（16 表幂等建表），**新增表打新包能生效，已有表加列/改列/数据迁移无效**（IF NOT EXISTS 跳过已存在表），且用户库在各自机器 data/，只能靠程序内迁移代码 ALTER。用户拍板推进：先做 schema 迁移框架（数据安全底座），再做在线更新。

**在线更新方案（已记 TODO，待做）**：清单驱动增量更新——更新服务器放 `latest.json`（版本 + 各文件 SHA-256 + URL），客户端检查对比版本，只下载 backend.jar（74MB）+ app.asar，jre/node/ffmpeg 带版本哈希仅变更时下载；壳 spawn java -jar 从 resources/app 启动 → 直接替换 jar 生效，data/ 不动。托管首选 Gitee Releases。配套 schema 迁移闭环（升级改表结构先发 vNN.sql）。

**阶段① 已交付（commit `9ce7eae`）**：`SqliteSchemaMigrator`（ApplicationRunner，仅 SQLite 数据源生效）：
- `PRAGMA user_version` 存版本；0（全新库/首次部署）直接初始化为最新
- 0<当前<最新：按序执行 `db/migration-sqlite/vNN.sql`（ScriptUtils 分割逐条，UTF-8），每步递增
- 最新版本 = max(基线 v1, migration-sqlite 目录最大 vNN)——以后改表结构加 vNN.sql 即自动提升
- 单测 5 项全过（版本扫描/全新初始化/老库按序迁移/已最新跳过/MySQL 跳过）；测试资源 v02/v03.sql 模拟真实迁移（生产打包不含）
- 约定文档 `backend/src/main/resources/db/migration-sqlite/README.md`

**阶段② 已交付（commit `1fae834`）**：桌面版在线更新（清单驱动增量，用户拍板：只 jar + 通用实现/配置占位）：
- **`desktop/update.js`**（纯 Node，无 electron 依赖）：语义化版本比较、清单拉取（http/https + 超时 + 1MB 上限）、流式下载 + SHA-256 校验、pending 标记（`data/updates/pending.json`）、`applyPendingUpdate`（备份旧 jar→复制新 jar→清标记）、`rollbackUpdate`
- **`desktop/main.js` 集成**：boot 时 `startBackend` 前应用待更新（jar 未被 Java 占用才能覆盖）；新版本启动失败→自动回滚 `.bak` 重试一次；主窗口后 5s 后台 `checkForUpdates`（native dialog 确认，失败静默）；当前版本用 `app.getVersion()`（package.json version）
- **`desktop/package.json`**：`updateUrl` 字段（空=关闭），build.files 纳入 update.js
- **`desktop/publish.bat`**：输出版本号 → PowerShell 算 jar SHA-256/大小 → 生成 `release/latest.json`（version + backend.url/sha256/size + changelog，URL 占位）→ 上传指引
- **node 直测 17 项全过**：版本比较（0.1.10>0.1.9）、清单拉取、下载+SHA（错误 SHA 拒绝）、pending 应用/目标不匹配跳过/备份/回滚
- **关键设计**：jar 被 Java 进程占用不能覆盖 → 下载校验后写 pending，下次启动应用；只替换 jar（业务+前端静态资源全在 jar），用户 data/ 不动；`latest.json` 缺 backend 配置/校验失败/网络失败全部静默降级，不影响正常使用
- **未做**：托管未配置（updateUrl 留空，功能关闭）；app.asar 更新（留接口未来）；更新 UI 为 native dialog（未接前端毛玻璃弹窗）
