# 工作日志 2026-09-09

## v0.26 会话进度保存

### 当前定位
- v0.26 已从“高光混剪工作台”调整为“单媒体 HTML 模板化推荐视频”。
- 用户选 Clip，系统负责素材检查、场景编排、HTML 预览和 MP4 输出。
- 高光工作台保留为素材选择、入出点调整、排序、剧透、音量和缺源处理工具。
- FFmpeg 只负责素材裁剪、转码、音频处理、封装和校验；不继续承担主要视觉卡片设计。
- `ScenePlan` 作为统一中间层；后续可扩展 HTML Renderer、FFmpeg Utilities 和 FCPXML/OTIO 导出。

### 已完成
- v0.26 主设计和唯一执行计划已建立并在 2026-09-08 提交。
- 单媒体 HTML 视觉原型已生成并用浏览器打开验证：
  - 作品档案片头；
  - 封面、简介、标签和统计；
  - Clip 播放与切换；
  - 集号/标题提示；
  - 章节提示；
  - 推荐回顾墙；
  - 播放进度和声音控制。
- 提交：`5b11843 feat: v0.26 media recommendation prototype and groundwork`。
- 定向测试 49 项通过；编译、Node 语法、静态资源同步和 `git diff --check` 通过。
- 当前原型文件：`docs/design/2026-09-08-v026-single-media-recommend-template.html`。
- 当前原型截图：`docs/design/2026-09-08-v026-single-media-recommend-template-preview.png`。

### G2 正式单媒体模板预览接入（2026-09-09）
- 新增 `RecommendSingleService` 和正式模板 `backend/src/main/resources/templates/recommend-single.html`；复用 `HighlightProject`、`HighlightProjectItem`、`MediaDetail`、集和 Clip，不新增选择/资产模型。
- 新增 `GET /api/highlight-projects/{id}/single-preview`，由现有工作台的「模板预览」按钮打开。
- 模板使用真实媒体标题、年份/季、简介、标签、封面回退、制作稿片段顺序、入出点和集号；本地封面缺失、外部资料缺失或没有片段时均保留完整降级页面。
- 就绪片段仅指向现有受控 `/items/{itemId}/preview` URL；服务端不把 `sourcePath` 输出到 HTML。未就绪片段保留在右侧清单并显示失败原因。
- 自动章节延续 `chaptersEnabled` 配置：跨集且片段达到阈值时展示；“始终显示”仍待补齐。
- 高光工作台现在区分「模板预览」（HTML 主体验）与「生成视频预览」（现有 FFmpeg 兜底），不破坏原有素材准备、缺源和 MP4 导出流程。

### 验证
- `mvn -f backend/pom.xml -o -Dtest=RecommendSingleServiceTest,RecommendServiceTest test` 通过：22 项，0 失败。
- `mvn -f backend/pom.xml -o test` 全量通过：372 项，0 失败、0 错误、0 跳过。
- `mvn -f backend/pom.xml -o -DskipTests compile`、`node --check backend/src/main/resources/static/app.js`、`git diff --check` 通过。
- 静态资源已同步到 `backend/target/classes/static/`，源码与 target 哈希一致。
- 使用隔离 18099 实例实际请求单媒体模板预览，返回 HTTP 200、`text/html;charset=UTF-8`；浏览器打开、全页截图和控制台错误检查通过，覆盖无封面/无简介/无片段降级态。验证实例已关闭。
- 8080 IDEA 实例是改动前启动的旧 class：新路由命中旧实例时返回静态资源 404，必须由用户重启后再做真实 Clip 的 Web/MP4 黄金路径验收。

### 单媒体模板内容版式重审（2026-09-09）
- 依据动漫自媒体推荐场景重排正式模板：作品钩子 → 作品档案 → 关键片段 → 推荐回顾，减少产品说明感和无效留白。
- 片头突出真实媒体标题、封面、年份/季、片段/集数；不再使用固定的“让作品自己开口说话”主文案。
- 简介只在作品档案区展示一次；没有简介、封面或片段时保留自然降级，不伪造推荐观点。
- 片段区改为内容主体，按制作稿顺序播放；章节按实际集号变化生成，缺源项保留在清单中并显示原因。
- 片尾改为片段回顾墙，减少“模板演示”式文字，保留回到开头和重新播放操作。
- 更新文件：`backend/src/main/resources/templates/recommend-single.html`；同步到 `backend/target/classes/templates/`。
- 隔离 18099 实例浏览器验收通过：空媒体/无封面/无简介/无片段页面正常打开，控制台无错误；截图输出 `backend/target/v026-single-preview-v2.png`。
- `RecommendSingleServiceTest` 通过：2 项，0 失败；`node --check` 和 `git diff --check` 通过。

### 独立二次元视觉样稿取消（2026-09-09）
- 按用户反馈，独立的“动漫杂志”视觉样稿未达到预期，已删除其 HTML、截图、提取画面与图标资产；不纳入 v0.26 正式模板或提交内容。
- 保留现有正式模板业务接入与已记录的后续工作，不以该样稿作为后续视觉方向。

### 当前未完成
- 模板预览与正式视频预览尚未共用同一份完整 `ScenePlan` 快照。
- “始终显示”章节策略、缺源的重新准备/上传/移除/当前可用版动作仍需收敛到模板主体验。
- 真实用户 Clip 的 HTML 模板预览、MP4 录制、Web/Electron 黄金路径，以及 Docker Testcontainers 全量测试仍待验收。

### 下一会话第一步
1. 用户重启 8080 后，从任意带已准备高光片段的媒体打开「制作推荐视频 → 模板预览」，验证片段播放、集号、章节和缺源提示。
2. 将 HTML 模板与正式预览/导出共享的 ScenePlan 快照抽出来，再实现模板录制输出。

### 约束
- 不重新设计高光工作台。
- 不新建第二套 Clip/VideoAsset 模型。
- 不先引入 Remotion、Skia、游戏引擎等新渲染栈。
- 不自动替用户选高光、删片或缩短用户已选片段。
- 默认无 BGM、MP4/1080P；高级选项折叠。
