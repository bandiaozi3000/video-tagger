# 2026-08-25 工作日志

## v0.21 独立片段放映式模板

- **决策**：章节式模板不继续承载新版 Clip 视觉；v0.21 新增独立 `recommend-clip.html`，与章节式并存。stream/overview 保留源码但不再作为正式 UI 选项，历史配置回退章节式。
- **片段模板流程**：开篇观前速览（媒体/Clip 统计与开场封面）→ 可选前言 → 媒体故事卡与 Clip 连播 → 结尾总览回顾墙。
- **兼容**：有 Clip 显示真实视频与角标/顺序 rail；无 Clip 显示同一视觉体系的静态回退；现有标题、前言、结尾、分组排序、背景、BGM、开场、详情显示、时长和速度配置继续透传。
- **实现**：新增模板、模板选择 UI（章节式/片段放映式）、历史值归一化、章节模板 HEAD 基线备份；补片段时长对视频录制预算的计算。
- **验证**：离线 compile、`RecommendControllerTest`/`RecommendServiceTest`、模板内嵌脚本语法均通过；浏览器已确认推荐向导模板选项和片段放映式选择。当前库无可用媒体/本地视频，真实 Clip HTML zip/MP4 仍待验收。



- **背景**：用户要求将“可一次执行完成的 v0.20 可扩展混剪最终版”规划内容归档到项目主 spec/plan，随后暂停本日任务。
- **已归档到主 spec**：
  - canonical Style Pack、版本/哈希与能力校验；
  - 内置 Renderer、受控外部风格包边界；
  - fixed/sidechain 音频模式及能力不足时的拒绝/fallback；
  - 统一音视频参数、转场时长限制、FFmpeg fallback；
  - 取消竞态、超时、文本转义、可解码校验、清理和磁盘门禁；
  - HTTP(S)/HLS/DASH 的 SSRF、大小、时长、鉴权、DRM 和无界流安全边界；
  - Release gate 与 Web/Electron 人工验收边界。
- **已归档到主 plan**：按 A（可靠性）→ B（风格/共享 Renderer）→ C（音频/流媒体）→ D（测试/发布门禁）的连续实施顺序，以及具体测试矩阵和必须拒绝的能力。
- **技术记录**：`docs/learn/technology.md` 已有精简版风格包驱动混剪链路记录；未新增冗余长文档。

**今日状态**：仅完成文档归档，未执行本轮最终版代码任务；工作区已有此前实现改动，未提交。后续从主 plan 的 A 阶段继续，不另建漂移计划。

## v0.21 推荐导出插入真实片段：接上次会话 + grill 定稿 + spec/plan 落盘

- **接续**：用户从上次会话记录（`ac57f034`，tender-greeting-rossum）恢复「导出推荐新增视频片段」设计，5 批 grill 前 4 批已拍板，补完末尾 3 个未决项。
- **新拍板**：角标=两者结合（勾选组合 + 自定义占位符 `{media}/{clip}/{i}`）；HTML 交付=zip 打包；瞬时片段（end_sec 空）=不设默认时长，播到源视频自然结束。
- **已有拍板**：附加详情展示；卡内小窗连播；插入真实片段视频；独立管理入口；数量不管控；存草稿+模板；HTML+资源文件夹；可拖拽排序；素材缺失拦截；未选片段媒体保持现状。
- **技术方案**：不建新表，`mediaClips` 存推荐草稿/模板 config JSON；HTML 版媒体卡小窗放 `<video src="media/clip-<id>.mp4">` + zip 导出；MP4 版 puppeteer 录屏直录真实片段（无需 ffmpeg 重拼接）；素材准备/校验复用 v0.19 `ClipExportService`/`LocalVideoResolver` + v0.20 产物保护。
- **落盘**：主 spec `docs/superpowers/specs/2026-08-25-video-tagger-v021-recommend-clip-design.md` + 唯一 plan `docs/superpowers/plans/2026-08-25-video-tagger-v021-recommend-clip-plan.md`。
- **版本**：用户拍板升 **v0.21**。
- **今日状态**：设计定稿，**暂缓开工**；后续从 v0.21 plan 的 Phase 1（后端数据链路）开始，开工前先核对 `ClipExportService` 对 end_sec 空行为与 `render.js` autoplay 参数。

## v0.21 最终推荐 HTML 视觉原型（待用户审阅）

- **产出**：`docs/design/2026-08-25-v021-recommend-clip-preview.html`，独立单文件、无外部资源，仅用于审阅最终导出 HTML 视觉，不接后端、不改业务页面。
- **展示**：深色霓虹毛玻璃放映页、开场标题、媒体封面叙事、卡内真实片段窗口占位、组合+自定义角标、片段顺序 rail、状态/备注/标签、无片段媒体静态回退、底部导览进度。
- **实际检查**：agent-browser 直接打开本地 HTML；桌面截图与 390px 窄屏截图均已生成；片段切换、自动导览暂停/继续、回到开场通过；窄屏 `scrollWidth === clientWidth === 375`，无横向溢出。
- **待用户**：审阅视觉原型后决定是否调整风格/布局，再恢复 v0.21 代码实施。

## v0.21 代码实施（接续 UI 审阅后）

- 后端：新增 `RecommendMediaClips`、`RecommendClipSourceService`，支持按媒体校验 Clip 归属、`videoFp` 本地源、时间范围，并裁剪到隔离 `media/clip-<id>.mp4`；无 `endSec` 时 ffmpeg 播到源视频结束。
- 推荐导出：新增 `/api/recommend/preview`、`/api/recommend/prepare-clips`；HTML 带片段时输出 HTML + `media/` zip，无片段继续返回旧单 HTML；视频异步任务透传片段配置。
- 模板：stream/chapter/overview 主详情卡接入卡内真实片段小窗、顺序 rail、角标占位符和连播时长；无片段媒体保持原静态卡；render.js 增加自动播放策略。
- 前端：推荐网格/列表卡增加「选片段」入口；新增 16:9 片段管理弹窗（候选、加入/移除、拖拽排序、角标组合+自定义占位符）；`mediaClips` 纳入草稿/模板和 HTML/视频请求；cache-bust 更新到 `20260825-v021`。
- 可靠性：SQLite 迁移器对已存在列幂等跳过；Clip 单视频导出允许无结束时间自然结束；预览/导出工作区失败清理；Spring 多构造器显式 `@Autowired`。
- 验证：离线 compile、RecommendService/RecommendVideoService/ClipExportService/SqliteSchemaMigrator 定向测试、RecommendControllerTest、Node 语法和 diff 检查通过；Spring Boot 已实际启动，推荐向导页面可打开。
- **待验收**：本机数据库无可用媒体/本地视频素材，尚未点击真实片段卡、生成真实 zip 或录制含片段 MP4；Web/Electron 真实媒体验收留待后续。

