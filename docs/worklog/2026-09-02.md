# 2026-09-02 工作日志

## Animeko 对照

- 使用 `D:\Tool\Ani\Ani.exe` 对照《少女与战车》第 2 集。
- Animeko 使用 Bangumi subject `40310`、episode `198749`，搜索 `少女与战车`、`GIRLS`、`ガルパン`，最终使用稀饭动漫 `xfvod` 的 `TV/02.mp4`。
- Animeko 播放器为 MPV/libmpv，确认 MP4、1920x1080、H.264 和约 24 分钟时长。
- 当前项目旧逻辑曾在第 2 集页面多个 MP4 地址中取首个地址，错误选择 `TV/03.mp4`；并混入剧场版、OVA、最终章和特典候选。

## 本轮实现

- `VideoSourceDiscoveryQuery` 增加作品/集外部 ID、集标题、季度集号和系列集号上下文。
- `VideoSourceTitleMatcher` 增加衍生作品排除和跨作品同集号拒绝。
- `AnimekoWebSelectorVideoSourceProvider` 按页面集号选择对应媒体地址。
- `VideoSourceQuickPlayService` 接入 Animeko `channelTiers`，排序优先匹配质量和可播放状态。
- Provider HTTP 查询和中继改用浏览器型 User-Agent；不转发 Cookie、Authorization 或任意自定义请求头。
- `SqliteSchemaMigrator` 对 `user_version=0` 遗留库按关键结构推断版本；旧 `clips` 易冲突索引延后到迁移后创建。
- 增加“无版本号新版任务数据保留”回归测试。

## 验证

- 定向 Maven 测试通过：`SqliteSchemaMigratorTest`、`VideoSourceTitleMatcherTest`、`RealVideoSourceProviderTest`、`VideoSourceQuickPlayServiceTest`、`VideoSourceRelayServiceTest`。
- 使用原数据库的隔离副本启动 Spring Boot 成功，原 `data/video_tagger.db` 未执行迁移。
- 当前工作区 `data/video_tagger.db` 的媒体 `5609` 当前没有 Episode `229`，因此本轮未对原库执行写入式快捷播放；端到端门禁保留到资料同步后复测。

## 取舍

本轮只完善“播放 → 选择线路 → Clip → 标签/备注”主链路，不加入弹幕、评论、共同观看、推荐流、字幕/音轨切换、HLS/DASH、MKV、DRM 或 MPV 原生播放器。

## v0.24 立项：素材渠道化 + Animeko 互操作（grill-me 拍板）

### 讨论 → 决策（grill-me 会话）

- 对照 Animeko（`D:\learn\myLearn\animeko`）源码逐项取证：`RememberPlayProgressExtension`（暂停即落盘 playhead、播放中 60s 周期写）、`CacheOnBtPlayExtension`（仅 BT 播放自动缓存整集）、`EpisodeCacheRequester`（显式缓存=进程内、无外部 API/CLI/深链）、`ani_room_database_main.db`（Room SQLite，`playback_history_record` 主键=Bangumi episodeId）。
- 拍板：播放前台移交 Animeko；video-tagger 只留两层薄依赖（只读其 SQLite + 定位其本地缓存文件）；素材层渠道化 C1 本地池 → C2 Animeko → C3 网页直链 → C4 录屏；v0.23 片源 UI 冻结渐进清理、引擎（下载/资产/中继）复用；Animeko fork CLI（`--test-task batch-cache`，<100 行）作可选批量预取加速器，不做 UI 自动化点按钮（Compose 自绘窗口不可靠）。
- 测试模型：契约驱动 + fixture（真实 DB 隔离副本），唯一需人工一次 = 真实 Animeko 播放校准。
- 版本：按 0.24 立项（0.23 已 5 提交封存基线）。
- 文档：主设计 `docs/superpowers/specs/2026-09-02-...-design.md`、实施计划 `docs/superpowers/plans/2026-09-02-...-plan.md`。

### M1（G2）实施：Animeko 观看导入

- 迁移：MySQL `V28__animeko_watch_import.sql` + SQLite `v10.sql`（main+test）`episode` 加 `watched_at`；`sqlite-schema.sql` 基线同步（fresh 库路径）；`SqliteSchemaMigratorTest` 版本断言 9→10 并补最小模拟库 episode 表（遗留/无版本/v5/v7 场景）。
- 后端：`Episode.watchedAt`；`AnimekoWatchImportService`（只读 Animeko sqlite `playback_history_record` → Bangumi id 桥 `external_work(provider=BANGUMI)` → `external_episode.provider_episode_id` → 本地 Episode 幂等 `updateById` 标记 watchedAt；未映射/无集分开计数，deleted 行过滤，单批 500，同集去重）；`AnimekoWatchController` `GET/POST /api/animeko/watch/{status,import}`；配置 `videotagger.animeko.db-path`（env `ANIMEKO_DB_PATH`）。
- 测试：`AnimekoWatchImportServiceTest`（4 项：未配置关闭/映射计数/同集去重/DB 不可达）+ 迁移测试 10 项全过；**全量 308 测试 BUILD SUCCESS**（基线 291 + 新增）。
- 踩坑：EpisodeMapper 文件为 CRLF 且纯注解，改用它继承的 MyBatis-Plus `updateById` 免动 mapper；Mockito `anyString` 兜底 stub 与精确 stub 并存结果不确定 → 只 stub 精确匹配、未命中默认 null；迁移最小模拟库需含 episode（v10 ALTER 目标）且按推断分支带 `media_entry_id`。

### 遗留/下一步

- M1 契约校准（一次人工）：真实 Animeko 播放→暂停→dump 比对字段。
- G3 M3 素材化管线渠道化、G4 M2 现场热键打标、G5 fork CLI（见 plan）。
- 测试基建：agent-browser CDP 本机重验、Animeko fixture 脚本。

### M1 验收：真实数据闭环（修正 M1v2 双源）

- Animeko DB 定位：`C:\Users\86132\AppData\Roaming\Him188\Ani\data\ani_room_database_main.db`（Room SQLite，列名 camelCase 与 reader 契约一致）。
- **真实数据发现（打脸"播放历史=已看存档"假设）**：`playback_history_record` 74 条中仅 1 条 active（Animeko 自清理，只留最近播放）；`episode_collection` 232 行但 `selfCollectionType='WATCHED'` 为 0（用户未开 Bangumi 云同步）；`torrent_cache`/`http_cache_download_state` 0 行（web 直链流播不留整文件）。→ 该用户 Animeko 当前**无持久已看存档**，只有"最近播放环"。
- **修正 M1v2**：import = 播放历史（最近观看/精确时间）∪ `episode_collection` WATCHED（开云同步后的持久已看，时间取 lastFetched）；跨源同集去重；旧版无收藏表时跳过。新增单测（收藏导入+跨源去重）。
- **闭环验证（副本，主库未动）**：45MB 真库副本迁移 v9→v10；造桥 40310/198748→episode#107 后 `POST /import` → `imported:1`，DB 内 `episode#107.watched_at=1788350140151` 与 Animeko 播放时间戳一致。
- 结论：M1 链路全通；真实产出依赖前置建档（同步中心给 Bangumi subject 建 MediaEntry/ExternalEpisode），与 09-02 遗留"资料同步后复测"门禁一致。
- C2 摸底：该用户无 BT/显式缓存文件（cache 表 0 行）→ M3 的 C2 文件定位需在用户用显式缓存/BT 后才有效。

### M3 素材化管线渠道化后端 + 详情页 UI 重构（本轮）

**后端 M3（提交 `e200202`）**
- 双库迁移 `clips.channel_hints`（JSON 数组渠道线索）：MySQL V29 + SQLite v11（main+test）+ sqlite-schema 基线 + `SqliteSchemaMigrator.inferVersion` 补 9/10 分支（顺带修正 M1 遗留：v10 结构无 user_version 遗留库的推断盲区）。
- 渠道模型：`MaterializationChannel`(C1 本地池/C2 Animeko/C3 网页直链/C4 录屏 + 优先级) + `ChannelHint`(state PRESENT/PENDING/UNAVAILABLE；Codec 用 **JsonNode 手写编解码**——踩坑：裸 `new ObjectMapper()` 序列化 record 报 "No properties discovered"，JsonNode 方案绕开 record 反射)。
- `MaterializationService`（C1→C4 求值编排 + persistHints 回写）：C1 经 clip.videoAssetId/videoFp 命中可用资产；C2 经 `external_episode.listByLocalEpisode` 反查 BANGUMI 桥 → `AnimekoCacheLocator.locate`；C3 http(s) URL → PENDING；兜底如实上报 C2 UNAVAILABLE/C4。
- `AnimekoCacheLocator`：读 Animeko `data/datastore/mediaCacheMetadataV2`（JSON，宽松字段匹配 episodeId + 递归收集 file/path 候选 + 多根目录探活）；registry 空= PENDING（可显式缓存/预取），文件缺失=UNAVAILABLE。
- `ClipMaterializationService.materializeFromFile(clipId, absPath)`：C2/C3 外部文件裁剪（抽公共 `cut()`，产物 GENERATED_CLIP + 时间映射，与本地一致）。原 `materialize`（TRIM_LOCAL_ASSET）保留兼容。
- Controller：`/api/clips/{id}/material` 增 `GET /channels`、`POST /channels/refresh`、`POST /materialize-from-file`、`GET /play-source`（受控流式返回，白名单 mp4/webm/mkv/mov/ts，无本地源 409）。
- 测试：迁移 10 项(11 断言) + `MaterializationServiceTest` 9 项（C1 asset/指纹、C2 present/pending/unconfigured、C3、C4、codec upsert/优先级、枚举）；**全量 318 测试 BUILD SUCCESS**。

**前端 UI + watched_at 贯通（提交 `cb9be0d`）**
- 详情页布局重构（CSS 尾部 override，不碰旧规则）：`.container` 1120→1360；三列主列加宽（实测 528→681px @1264 窗口）；`.episode-group-list` 双列网格（296px min，12 集列表高 1212→656px 省 46%）；集行紧凑（封面 72、内边距收、窄格内操作区自动换行独立成行）；外部简介 4 行折叠 + 点击展开（事件委托 `.external-description.expanded`）。
- watched_at 贯通：`EpisodeSummary`/`EpisodeDetail` 加字段 + SQL 补 `e.watched_at AS watchedAt`；集列表行与集详情头显示「✓ 已看」徽章（M1 的 UI 落地）。
- 片段详情渠道感知：素材行显示渠道求值状态徽章；「准备素材」按钮按 /channels 分流 C1(TRIM_LOCAL_ASSET→materialize) / C2(TRIM_EXTERNAL_FILE→materialize-from-file)；「本地回顾」按钮 → play-source blob 弹窗播放并 seek 片段起点。
- 版本号 index.html `?v=` bump 至 20260902-v024-m3b。

**验证**
- 渠道 API 契约（隔离副本 18099，未碰主库/Animeko）：clip 90 无资产 → C3 PENDING(bilibili URL) + play-source 409；造本地 asset 绑定后 → C1 PRESENT + filePath 命中 + play-source 200。
- UI 布局：主库 8080（前端已同步 target/classes，刷新即见新布局，agent-browser 量化 12 集双列）；**后端 class 改动需在 IDEA 重启 8080 调试实例后生效**（/channels、play-source、已看徽章数据）。
- 遗留：主库「少女与战车」无外部资料（external-metadata 需同步过才有简介折叠可看）；已看徽章要主库 Animeko 导入出 watched_at 数据后才显示。

### M2/G4 现场热键打标（Animeko 暂停 → 读 playhead → 建 Clip）（提交 `a165d7d`）

**后端**
- `AnimekoTagService`：直连 Animeko SQLite 读最新有效播放记录（暂停即落盘已取证）→ `external_work(BANGUMI, subjectId)` → `external_episode(provider_episode_id=episodeId)` → 本地 Episode → 建 Clip（url=`animeko://bangumi/{subject}/ep/{ep}` 稳定占位引用；startMs=播放头位置默认，endMs 可空=瞬时点；标签多词拆分关联 + embedding 入队）→ 幂等标记 watchedAt → 渠道求值 refresh 回写 C2 channel_hints。
- 桥键=**Bangumi episodeId**（=playback_history.episodeId），与 M1/M3 一致。
- Controller：`GET /api/animeko/watch/playhead`（mapped/unmapped 视图）、`POST /api/animeko/watch/tag`（返回 OK/NEED_ARCHIVE/NO_PLAYBACK/UNREACHABLE/BAD_REQUEST）。
- `MaterializationService` 修正：refreshHints 时 C2 **PENDING 也回写** channel_hints（可预取线索落库，之前只写 PRESENT）。

**前端/桌面**
- `animeko-tag.html` 浮层页（Electron 浮层窗与 web 弹窗共用）：显示当前媒体/集/进度/暂停时刻 + 位置漂移提醒（updatedAt 距今 >15s 提示先暂停）；标签/备注/起点(默认=播放头)/终点 → 保存 → 结果含渠道状态。
- `desktop/main.js`：`Ctrl+Alt+T` 全局热键（CommandOrControl+Alt+T）→ alwaysOnTop 无边框 skipTaskbar 浮层小窗（后端未就绪弹提示）；`will-quit` unregisterAll。
- web 主导航加「互操作 → Animeko 打标」按钮（无 Electron 时 `window.open` 独立小窗）。

**验证**
- 单测 6 项 + 全量 **324 BUILD SUCCESS**。
- 隔离副本真实验收：playhead 读**真实 Animeko 库**成功（用户连播《少女与战车》40310，位置随 60s 周期漂移、逐集递增 → 证实"暂停才固定"语义 + 未建档集 NEED_ARCHIVE 引导正确）；造桥（246634→ep90）后 `POST /tag` → clip#164：start_ms=播放头、channel_hints=[C2 PENDING]、episode watched_at 落库。
- 结论：M2 链路全通；真实使用依赖「先建档（同步中心给 Bangumi subject 建档绑定集）」。剩一次人工校准剧本（真实暂停→浮层打标→抽查位置）。

### 遗留（v0.24）
- G5 Animeko fork CLI（可选加速器，--test-task batch-cache）
- G6 收尾：v0.23 片源/播放 UI 从主入口隐藏（功能开关）、CHANGELOG 0.24.0、端到端复测（资料同步后 + M2 人工剧本）
- 人工校准：真实 Animeko 暂停打标剧本一次
- 主库需重启（IDEA 8080）后：Animeko 打标入口/浮层、M2 API、M3 渠道 UI、已看徽章全量生效

### M2 补充：全局热键托盘（不依赖桌面主壳/web，独立常驻弹打标窗）

- 用户诉求：不依赖桌面壳或 web 页面，按快捷键直接弹 Animeko 打标窗。
- 技术事实：系统级热键必须有常驻进程；浏览器沙箱无法注册全局热键。
- 交付 `desktop/tag-tray.js` + `desktop/tag-tray.bat`：
  - 独立 Electron 托盘程序（无主窗口，仅托盘图标 + 全局热键）
  - 启动后常驻，`Ctrl+Alt+T`（CommandOrControl+Alt+T，可用 VT_TAG_HOTKEY 换键）→ 探活后端 8080 → alwaysOnTop 浮层窗加载 /animeko-tag.html
  - 托盘左键=弹窗，右键菜单=退出；单实例锁（重复启动唤起旧实例）；后端未就绪弹窗提示
  - 托盘图标程序化生成（16×16 紫圆 PNG，node zlib 手写 PNG 编码，无资源文件依赖）
  - `VT_BACKEND_PORT` 可配后端端口
- 环境修复：electron 32.3.3 安装走 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`（github 直连被断 ECONNRESET；npm registry 镜像元数据 200 可用）。本机 electron Cache 已有 v32.3.3 zip（108MB，验证下载过）。
- 验证：`node --check` 语法 OK；electron 实跑 5s 打印「常驻就绪：CommandOrControl+Alt+T」无崩溃（GPU 报错为 timeout 强杀噪音）。GUI 托盘交互留用户双击 bat 人工验收。

### M2 补充2：托盘启动器改 VBS 无窗口（用户关 cmd 窗口导致 electron 连带退出）

- 问题：tag-tray.bat 用 start 拉起 electron，electron 是 cmd 子进程 → 用户关闭弹出的黑窗口即连带终止托盘（圆点消失）。
- 修复：新增 `desktop/start-tag-tray.vbs`（纯 ASCII + CRLF）：`shell.Run(..., 0, False)` 隐藏窗口启动 electron，wscript 立即退出，electron 完全脱离无窗口可关。重复双击由 tag-tray.js 单实例锁唤起旧实例。
- 实测：cscript 返回 0 + electron 3 进程存活 → 进程独立性验证通过。
- 保留 tag-tray.bat 作为备选（仍可双击，但关窗会连带终止，提示用 vbs）。

### M2 修复：浮层窗可拖动 + C2 UNAVAILABLE 渠道线索回写 + 端到端主库验证

- Bug：Animeko 打标浮层是 Electron 无边框窗（frame:false），animeko-tag.html 未设 `-webkit-app-region: drag` → 无法移动。
  修复：`.tag-head` + `h1` 设 drag、`.tag-close` 设 no-drag（web 浏览器打开忽略该属性无副作用）。
- 补漏：`MaterializationService` C2 UNAVAILABLE 分支此前不写 channel_hints（PRESENT/PENDING 都写）→ refresh 时一律回写（channel_hints 落库线索完整）。
- 端到端主库（8080，用户重启后）验证全通：
  - playhead → mapped:true（主库已建档 bridge 40310/198749→episode229《战车，搭乘了！》）
  - 弹窗页面显示建档态可填表单（少女与战车·S0-Ep2·03:41 + 漂移提醒）
  - 真实打标 POST /tag → clip#166：url=animeko://bangumi/40310/ep/198749、startMs=221137=播放头位置、区间221137–221500、标签"验证 战车道"拆分关联、episode.watchedAt=Animeko 时间戳回填
  - 空标签请求正确 BAD_REQUEST 不写库

### M2 交互修复：最小化/缓存/起终点标记

- 最小化不生效：tag-tray.js 缺 ipcMain 监听 → 注册 win:minimize→hide（收起，热键/托盘再唤出）、win:close→close；openTagWindow 处理 hidden 窗口 show。
- Electron 缓存旧页面致改动不生效：openTagWindow loadURL 加 `?_ts=${Date.now()}` 绕过页面缓存。
- 起终点交互重构（animeko-tag.html 重写）：
  - 模式切换：单点标记 / 区间片段；区间模式显示终点行。
  - 起点/终点各自：−5s/−1s/＝暂停位置/+1s/+5s（秒级微调，毫秒精度），时间实时格式化 mm:ss。
  - 保存：单点且起点=暂停位置 → 不传 startMs（后端取最新落盘）；区间 → 带 startMs/endMs；打标成功自动收起（Electron），web 环境 1.2s 后复位。
  - 头部分「–收起」「✕关闭」双窗控；进度显示暂停时刻+漂移警告（点＝暂停位置同步）。
- 主库验证：单点默认=暂停位置 00:05:33、切区间终点自动=起点+3s(05:39)、＋3s 微调、＝对齐、往返切换全通。

### M2 修复：保存后误用 close（销毁窗口）→ 改 minimize（收起）

- 保存成功分支原调 window.vtDesktop.close()（IPC win:close → BrowserWindow.close 销毁），用户看到"保存直接退出"。
- 改：Electron 端成功显示 1.4s 后 vtDesktop.minimize()（IPC win:minimize → hide 收起不销毁），200ms 即复位表单；web 端 1.6s 复位可继续。
- ✕ 按钮仍真关闭；「–」收起。

### M2 增强：保存自动抽帧为片段封面（"当前画面"封面）

- 新增 `ClipFrameGrabber`（@Component）：渠道文件在场（C1 本地池/C2 Animeko 缓存 PRESENT）时，ffmpeg 从整集文件在打标时刻抽帧 → CoverService 存 clip/{id}.jpg + {id}-xl.jpg → 回写 coverPath/detailCoverPath。独立守护线程异步，失败静默（可稍后手动补封面）。
- AnimekoTagService.tag 成功 + 渠道 PRESENT → 触发抽帧（抽帧时刻=用户标定起点，无起点=暂停位置；clamp 到集时长-500ms）。
- 修复三个抽帧 bug：
  1. ffmpeg 老版(2018) banner 撑爆 readNBytes 截断 → readAllBytes + `-hide_banner`
  2. readAllBytes 在 waitFor 前会永久阻塞挂起 → 先 waitFor(90s) 再读流
  3. `-ss` 超集时长导致 exit=0 空输出（temp 无文件）→ 起点 clamp 到 durationMs-500
  4. 临时输出路径从系统 %TEMP% 改为 coverDir/clip/{id}-grab.part.jpg（同盘落位）
- C1 求值补充：Animeko 打标 clip（无 videoAssetId/指纹匹配）按 episodeId 命中可用本地资产（assetMapper.selectAvailable + isLocalKind），补单测 c1ByEpisodeWhenNoBinding。
- 端到端（副本+真实视频文件）：tag → C1 PRESENT → 「片段封面已生成 clip=169 @30000ms」→ coverPath/169.jpg + 169-xl.jpg 落库。
- 主库 covers 调试孤文件已清理。
- 全量 326 BUILD SUCCESS。

### M2 增强：网页流封面兜底 = 打标后自动截屏当前画面

- 用户反馈核心是网页流无整文件 → ffmpeg 抽帧不可用。
- 方案 A（Electron 专属）：tag-tray.js 主进程 desktopCapturer 截主显示器（thumbnail 1920x1080 → toJPEG(88) → base64）→ IPC `tag:capture-screen`；preload 暴露 vtDesktop.captureScreen()。
- 后端 `POST /api/clips/{id}/cover`（body=base64）→ ClipService.setCoverFromDataUrl → CoverService 存 clip/{id}.jpg + {id}-xl.jpg + 回写 coverPath/detailCoverPath。
- animeko-tag.html：保存成功且渠道非 PRESENT（无本地文件）→ 自动截屏 → 上传封面 → 显示"封面已截取/失败提示"，1.8s 后收起。
- 实测：desktopCapturer 截屏成功（64KB JPEG）；toJPEG 返回 Buffer → 转 base64 过 IPC（踩坑：Buffer 无法直接 contextBridge）。
- 注意：截的是整屏（Animeko 播放画面在前台时即当前帧）；可能含播放器 UI/弹幕，非 DRM 黑屏。
- 全量 326 BUILD SUCCESS。

### M2 封面：窗口级截图（用户可选播放器窗口，非整屏）

- 需求：截屏只针对 Animeko/播放器窗口本身，不带桌面/弹幕。
- desktopCapturer types:['window'] 可行——实测精确截到 bilibili 播放窗口（187KB JPEG）。
- tag-tray.js 增 IPC：tag:list-windows（可见窗口 id+name）、tag:capture-window(id)（1920x1080 thumbnail→JPEG base64；窗口不可捕获回退 screen 由前端提示）。
- preload：vtDesktop.listWindows()/captureWindow(id)。
- animeko-tag.html：表单加"封面截图源"下拉（整屏 + 当前可见窗口列表，Electron 专属），选择存 localStorage（vt-animeko-shot-source），保存时按所选源截帧上传；窗口失效自动回退整屏并提示。
- 实测：窗口枚举 5 个可见、最大窗口（播放页）截图成功。

### G6 收尾：v0.23 片源 UI 隐藏 + CHANGELOG 0.24.0

- `videotagger.ui.show-v023-player`（env VT_SHOW_V023_PLAYER，默认 false）+ UiConfigController `/api/app/ui-config`。
- 前端 applyUiConfig()：false 时隐藏 3 处入口（设置侧栏"数据源与播放"、设置 section、集详情"自动查找并播放"）+ 全局 [data-v023-entry]（v022"管理片源"动态渲染按钮）CSSStyleSheet 规则。
- 验证：默认 false → 三处 hidden + 规则激活；VT_SHOW_V023_PLAYER=true → 全部恢复可见。引擎层（v023 模块/下载/资产/受控中继）零改动保留。
- CHANGELOG 0.24.0 版本块。全量 326 BUILD SUCCESS。

### M2+ 批量物化（延迟物化编排）：勾选片段→剪产物；无文件→待预取清单

- `MaterializeBatchService` + `POST /api/clips/materialize-batch` {clipIds}：
  - 逐个 evaluate(refresh)：本地文件在场（已绑资产 TRIM_LOCAL_ASSET→materialize / 按集兜底命中的外部文件→materializeFromFile）→ 立即 ffmpeg 剪出产物
  - 无整文件 → 反查 BANGUMI subject/episode → 汇总 prefetchNeeded（按集去重），供 Animeko 显式缓存/fork CLI 预取后回来重跑
  - 未建档（无法反查）→ 明确提示
- 前端：媒体详情「素材概览」加"批量物化"按钮 → modal：该媒体片段勾选列表 → 执行 → 结果分组显示（✓已剪 / ✗失败 / ⚠待预取 subject/ep 清单）
- 修两个裁剪 bug：
  1. C1 按集兜底命中的 clip（无 videoAssetId）走 materialize() 会因 plan 判非 TRIM_LOCAL_ASSET 失败 → 改 materializeFromFile(filePath)
  2. VideoAssetPathService.assetPath 要求 mediaId/entryId≥1 导致外部文件路径炸 → 放宽（episodeId/assetId≥1 即可，0 归一化 media-0/entry-0）
  3. materializeFromFile 无真实源资产 → 跳过 video_time_mapping 插入（旧资产映射无意义）
- 单测 4 项（已绑资产剪/按集兜底剪/不在场去重待预取/未建档提示），全量 330 BUILD SUCCESS。
- 端到端（副本+真实视频）：clip90 设区间→批量物化→产物 asset-4.mp4 786KB 6.07s（区间 1.0–6.0s 精确裁剪，ffmpeg 校验通过）；无文件时正确给待预取 subject40310/ep198749。

### C2 定位器真实数据修复（用户显式缓存后验证）

- 用户问题"如何判断本地有文件"触发真实缓存结构核查 → 发现 Animeko registry **无 file/path 字段**：条目 = { origin:{mediaId...}, metadata:{episodeId...}, engine:"web-m3u" }，文件实际按 `media-downloads/{engine}/{origin.mediaId}.{ext}` 布局命名。
- 修复 AnimekoCacheLocator：
  1. engineFileCandidates()：engine 目录 + origin.mediaId 递归推导候选（兼容 mp4/mkv/webm...）
  2. matchesEpisode() 递归进嵌套对象（episodeId 在 metadata 内，原只查顶层字段）
  3. MaterializationService C2 locator 改用 AnimekoPaths.resolve（与 Import/Tag 一致）——否则自动探测场景 C2 永远未配置
- 真实端到端（用户已缓存少女与战车第2集 → registry+web-m3u 文件在场）：channels → C2 PRESENT 命中真实文件 → 批量物化 → asset-1.mp4 6.47s H.264 1080p 剪出。
- 单测：AnimekoCacheLocatorTest 4 项（真实结构命中/文件缺失 PENDING/无条目/未配置）；全量 334 BUILD SUCCESS。

### M2 物化时间语义修复 + source_start_ms 保留原位置

- 用户反馈：物化后详情显示"0–72s"疑为显示 bug → 实为 v0.23 既有语义（cut 归零），非 bug；为体验新增 source_start_ms 保留物化前原视频起点。
- 双库迁移 V30/v12：clips.source_start_ms；基线 + 推断分支(11)；迁移测试断言 11→12。
- ClipMaterializationService.cut：归零前若 sourceStartMs 未设则存旧 startMs（幂等，重物化不覆盖）。
- 前端 clip 详情：物化产物标注「· 原视频 HH:MM:SS 处」。
- 端到端（副本）：clip90 区间 5–12s 物化 → startMs=0/endMs=7000 + sourceStartMs=5000 保留。
- 全量 335 BUILD SUCCESS。
