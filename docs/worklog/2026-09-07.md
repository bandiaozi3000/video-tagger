# 2026-09-07

## Pi 中转 GPT 额度状态

- 用户确认执行后，检查 `~/.pi/agent/models-store.json`，确认当前 `openai/gpt-6-astra` 实际使用 `https://codex.wlbclub.com`，而非 OpenAI 官方地址。
- 对中转站 `GET /v1/usage` 做脱敏实测，返回 HTTP 200，确认其为 Sub2API 的 `quota_limited` 响应：含 `rate_limits`、状态与密钥到期时间，不是传统钱包余额。
- 更新 `~/.pi/agent/extensions/balance-status.ts`：
  - 新增 OpenAI/Sub2API 适配器，查询地址跟随当前模型的 `baseUrl`；
  - 底栏显示最先耗尽的套餐窗口，例如“日额度 $48.05”，不误称为余额；
  - `/balance` 显示所有额度窗口和有效期；
  - 缓存和低额度提醒按 `provider + baseUrl` 隔离；模型切换后，在途旧请求不覆盖当前状态，补刷使用最新模型地址。
- 验证：Pi 显式加载扩展并执行 `--list-models gpt-6-astra` 成功；真实接口返回解析结果确认存在日、7 日额度与到期时间。环境未安装可直接调用的 TypeScript `tsc`，因此未执行独立 TypeScript 类型检查。

## Pi 账户详情面板

- 用户选择在编辑器下方持续展示完整实时数据，而非将内容挤入 footer。
- 扩展新增 `balance-details` 下方 widget：显示状态、模式、当前额度、所有额度窗口与重置时间、密钥到期时间、RPM/TPM/平均耗时、今日与累计 token/费用、各模型累计和每日用量明细。
- footer 仍保留最紧额度窗口的短摘要；查询失败时详情面板保留最后一次成功数据并标记刷新失败，不会泄露认证信息。
- 验证：中转站 HTTP 200 响应确认含 2 个额度窗口、5 条模型累计、12 条每日明细及所有面板依赖字段；Pi 再次显式加载扩展成功。

## Pi 面板信息密度调整

- 用户反馈常驻完整统计信息过多，改为下方默认仅显示状态、模式、当前及全部额度窗口与重置时间、到期时间、今日请求数和费用。
- `/balance` 改为手动刷新并展开完整统计；下一次普通自动刷新恢复基本信息，footer 始终只保留最紧额度摘要。
- 验证：真实中转响应含默认面板所需状态、2 个额度窗口、到期时间和今日请求/费用字段；更新后的扩展可由 Pi 成功加载。
